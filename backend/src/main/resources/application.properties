spring.application.name=cashflow-backend

# Perfil activo por defecto en desarrollo local.
# En despliegue (Render, VPS, etc.) se sobreescribe con la variable de entorno SPRING_PROFILES_ACTIVE=prod
spring.profiles.active=dev

server.port=8080
